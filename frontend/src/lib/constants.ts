import { dev } from "$app/environment";

export let baseUrl = "https://bouvet.billypool.no/api";
export let isMultipleTenant = true;

// if (dev) {
//     baseUrl = import.meta.env.VITE_API_URL;
// }
